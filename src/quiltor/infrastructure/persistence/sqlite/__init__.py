"""Focused SQLite adapters for Quiltor's persisted aggregates.

Callers import the concrete module they need; this package deliberately does
not recreate the retired all-purpose ``storage`` namespace.
"""

__all__: list[str] = []
