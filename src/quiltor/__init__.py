"""Quiltor's Python application package.

The package follows a ``src`` layout so installed artifacts and source checkouts
exercise the same import graph. Executable composition roots live in
``quiltor.hosts``; domain code never reaches out to them.
"""

__all__: list[str] = []
