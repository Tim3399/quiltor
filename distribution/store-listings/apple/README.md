# Apple listings

App Store Connect metadata is owned by target. macOS lives under `macos/` and
iOS under `ios/`; neither target may consume the other's screenshots, privacy
answers or application records. Truly shared copy can be introduced under a
future `shared/` directory and must be copied into each target's reviewed output.
