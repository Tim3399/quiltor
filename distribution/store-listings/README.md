# Store listings

Store-facing copy and assets live here because they have a different review and
localisation lifecycle from the application UI. Locale directories use BCP 47
tags. A target can move from `scaffold` to `supported` only after its required
listing fields and screenshots are checked by that store's publishing workflow.

Do not place signing credentials, Partner Center identities, provisioning
profiles or upload keys in this directory.
